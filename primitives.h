#include <stdio.h>
#include"types.h"

sexpr add_valisp(sexpr liste , sexpr env);
sexpr defvar_valisp(sexpr liste, sexpr env);
sexpr setq_valisp(sexpr liste,sexpr env);
void test_nb_parametres(sexpr s,sexpr c , int n);
